# GitHub Pages serves this repository's root publicly, so anything committed here
# is reachable at a guessable URL. The client's working files are kept local:
# source copy decks and the review mockup are not site assets.
assets/*.pdf
assets/dropbox files from Mike/

# Internal build notes — this repo root is served publicly, so the handoff
# document does not belong in it.
HANDOFF.md

# Unreferenced image from the original build; kept locally, not published.
assets/IMG_6337.JPG
